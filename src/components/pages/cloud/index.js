import React, { Component } from "react";
import { Container } from "react-bootstrap";
import MapChart from "components/pages/cloud/Map";

class Cloud extends Component {
    render() {
        return (
            <Container>
                <MapChart />
            </Container>
        );
    }
}
export default Cloud;
