import * as React from 'react';
import { Box } from '@chakra-ui/core';

import type { BoxProps } from './types';

export const HeaderMobile = (props: BoxProps) => {
  return <Box {...props}></Box>;
};
